var isCompoundWord = function(word) {
  //take every iteration 1 or 2
  var slices = [];

  //fro

  for (var i = 0 ; i < word.length; i++){
    word.slice()
  }

  
  return word.reduce(function() {

  }, false);
}